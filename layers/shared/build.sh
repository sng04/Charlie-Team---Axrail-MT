#!/bin/bash
# Shared utilities layer — pure Python, no pip dependencies.
# The python/ directory already contains the modules; nothing to install.
echo "Shared layer: no build step required (pure Python modules)."
